### Semantic Kernel Demo via Azure Foundry ###

## Pre-requisities ##
Use of Azure Foundry Agents (relies on Azure OpenAI (GPT-4o, GPT-4o-mini, Llama, Mistral) at this point in time for the native support of building with the UI/UX
For the example shown I've used the programmatic SDK for the Function Calling outside of Grounding Bing Response.

## Requirements ##
```bash
python3 -m venv venv
```

Then activate your environment
```bash
cd Scripts/Activate.ps1
```

Create example from .env.example in your local directory
!!! Don't commit to a repo this is internal use
```bash
.env (populate with .env.example) variables
```

